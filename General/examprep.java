package General;

public class examprep {
    public static void main(String[] args) {

        for (double d = 0; d < 1.1; d = d + 0.1) {
            System.out.printf("%.1f", d);
        }

        // int subtotal = 38 + 40 + 30;
        // double tax = 0.08;
        // double tip = 0.15;
        // double total = subtotal + subtotal * tax + subtotal * tip;

        // String x = "\"Music makes one feel so romantic \n - at least it always gets
        // on one's nerves - \n which is the same thing nowadays.\"";
        // String x1 = "A \"quoted\" String is \n'much' better if you learn \n the rules
        // of \"escape sequences.\"";
        // String x2 = "Also,\"\" represents an empty string.\nDon't forget:use \\\"
        // instead of \" !\n'' is not the same as \" ";
        // System.out.println(x2);
        // System.out.println(0.1 + 0.2);

    }
}
